{
  "name": "@dt/bff",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "echo 'bff uses e2e via curl, no unit tests in V1'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@dt/config": "workspace:*",
    "@dt/contracts": "workspace:*",
    "@dt/observability": "workspace:*",
    "@dt/realtime": "workspace:*",
    "@hono/node-server": "^2.0.0",
    "hono": "^4.6.12",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
