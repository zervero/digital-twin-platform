# Copy to .env and adjust. docker-compose.yml reads these.
# AUTH_PROVIDER is one of: mock | oidc. Production refuses
# to boot if it's unset or anything else.
AUTH_PROVIDER=mock

# LOG_LEVEL is one of: debug | info | warn | error.
LOG_LEVEL=info
