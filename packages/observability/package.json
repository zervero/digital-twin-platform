{
  "name": "@dt/observability",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "echo 'observability gets logger tests in V2'",
    "typecheck": "tsc --noEmit",
    "build": "mkdir -p dist && echo 'observability exports types only in V1' > dist/.gitkeep",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@dt/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
