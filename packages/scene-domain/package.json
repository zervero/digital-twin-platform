{
  "name": "@dt/scene-domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "mkdir -p dist && echo 'scene-domain is type-only' > dist/.gitkeep",
    "clean": "rm -rf dist .turbo coverage"
  },
  "dependencies": {
    "@dt/contracts": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.5",
    "typescript": "^5.6.3"
  }
}
